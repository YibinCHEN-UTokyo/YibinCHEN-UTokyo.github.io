title: "Final Report of Visual Media"
author: "CHEN Yibin"
description: "This open blog is served as the final report (Choice 2) for the course Visual Media." # This will show up in places like twitter, linkedin, etc, if you share your sites url as a post.

github_username: "YibinCHEN-UTokyo"

#########################################################################################
######### Nothing below needs to be changed (unless you know what you're doing) #########
#########################################################################################
theme: "minima"

rss: rss

kramdown:
  syntax_highlighter_opts:
    disable: true
    
plugins:
  - jekyll-feed
  - jekyll-sitemap

titles_from_headings:
  strip_title: true
  collections: true

defaults:
  - scope:
      path: ""
      type: post
    values:
      tags: Other
